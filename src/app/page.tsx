export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="surface max-w-xl space-y-4 p-8">
        <p className="eyebrow">ORDURA</p>
        <h1 className="text-3xl font-semibold tracking-tight text-slate-950">
          Frontend foundation is ready.
        </h1>
        <p className="max-w-prose text-sm leading-6 text-slate-600">
          The application shell, cookie-auth API foundation, and shared UI
          primitives are in place for the next delivery phase.
        </p>
      </section>
    </main>
  );
}
