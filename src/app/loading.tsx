export default function GlobalLoading() {
  return (
    <main className="page-shell" aria-busy="true" aria-live="polite">
      <p className="text-sm font-medium text-slate-600">Loading ORDURA…</p>
    </main>
  );
}
